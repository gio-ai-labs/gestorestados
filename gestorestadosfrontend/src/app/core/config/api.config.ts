/**
 * Configuracion base de la API REST del backend Gestor de Estados.
 * El backend corre con context-path /gestorestados (ver application.properties).
 */
export const API_BASE_URL = 'http://localhost:8080/gestorestados/api/v1';
